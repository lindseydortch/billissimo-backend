model user {
  id                      String                @id @default(uuid())
  first_name              String
  last_name               String
  email                   String                @unique
  image                   String
}